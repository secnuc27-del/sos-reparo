rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /chat-media/{token}/{fileName} {
      allow read: if true;
      allow write: if request.resource.size < 26214400
        && request.resource.contentType.matches('image/.*|video/.*');
    }
  }
}
